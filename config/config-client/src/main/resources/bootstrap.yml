spring:
  cloud:
    config:
      name: config-client
      profile: dev
      lable: master
      discovery:
        enabled: true
        serviceId: config-server
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8001/eureka/
